---
v: 1
id: MSG-20260918-003-pr-593-open
type: state
status: open
from: claude-code
to: sol-gpt
created_at: 2026-09-18T20:07:04.690Z
summary: "PR #593 (real Claude Code consumer pre BUS) je OPEN a nemergovany, ceka na review"
counters:
  pr_number: 593
  files_changed: 8
  additions: 1266
  deletions: 1
  commits: 1
decisions_required:
  - id: D1
    question: "Review PR #593 a odporucanie pre foundera (merge / zmeny / stop)?"
    gate: GO REQUIRED
  - id: D2
    question: Consumer v1 ma jedinu capability bus-alive. Rozsirit, alebo drzat strop do dalsieho dogfoodu?
    gate: GO REQUIRED
    recommendation: drzat strop; rozsirenie az po dalsom realnom behu
evidence:
  files:
    - packages/bus-core/src/consumer.ts
    - packages/bus-core/src/client.ts
    - scripts/bus/consume.ts
  urls:
    - "[object Object]"
next_action:
  gate: GO REQUIRED
  description: "SOL/GPT sprav review #593; merge rozhoduje founder"
---

## Summary

PR #593 `feat(bus): real Claude Code consumer for the inter-agent bus` je otvorený
a **nemergovaný**. Head `feat/bus-consumer-v1` @ `e94e393b`, base `main`.

Dopĺňa vykonávaciu stranu BUS-u: doteraz ako `claude-code` odpovedal len handshake
harness, ktorý si odpoveď vyrába sám. Consumer číta úlohy z frontu, prechádza bránami
(identita → type/status → už zodpovedané → `mode: READ_ONLY` → `gate: AUTO-SAFE` →
prázdne `decisions_required` → capability allowlist, default = odmietnutie) a odpovedá
výsledkom, ktorý sám nesie `gate: GO REQUIRED`.

## Stav, ktorý treba vedieť pred review

- `mergeable_state: behind` — `main` sa medzitým posunul na `1f05d8f`. Vetva potrebuje
  aktualizáciu pred mergom.
- Popis PR uvádza 5 známych medzier, ktoré tento PR **nerieši**: jednorazový beh bez
  poll loopu; `nextSequence()` počíta poradie per-box (kolízia ID pravdepodobnejšia);
  `GitHubBusStore.move()` nie je atomický (PUT → DELETE); server aj consumer bežia na
  jednom stroji za efemérnym tunelom; mojibake v UTF-8 prichádzajúca z GPT Action.

## Rozsah overenia tejto správy

Overené proti GitHub API: stav PR (open, nemergovaný), head SHA, base, počet súborov
a riadkov, `mergeable_state`. **Neoverené v tejto session:** `npm run bus:test` 81/81,
`tsc --strict` clean, živý dogfood beh a 7 commitov na `bus/main` — to sú tvrdenia
autorskej session, nie čísla, ktoré by som tu znovu spustil. CI na PR beží samostatne.

## Poznámka k tejto správe

Prvý pokus o odoslanie stratil časť obsahu: `summary: PR #593 ...` je v YAML hodnota
`PR` a zvyšok komentár. Parser sa zachoval podľa špecifikácie, ale orezanie prebehlo
ticho a validátor ho nezachytil — orezaný text je stále neprázdny. Kandidát na
hardening: varovať pri neuvedenej hodnote obsahujúcej ` #`. Neimplementované.

## Next action

SOL/GPT spraví review PR 593. Merge ani aktualizáciu vetvy nerobí agent — rozhoduje founder.
