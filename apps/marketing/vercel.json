{
  "ignoreCommand": "BASE=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\"; git cat-file -e \"$BASE\" 2>/dev/null || exit 1; git diff --quiet \"$BASE\" HEAD -- . && exit 0 || exit 1",
  "buildCommand": "next build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install"
}
