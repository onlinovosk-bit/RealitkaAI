// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  turbopack: {
    root: __dirname,
  },
  // Tu môžeš pridať ďalšie nastavenia podľa potreby
};

module.exports = nextConfig;
