# Live sync — S0.4 + S0.5 proti test MCC 7024414113

**Dátum:** 2026-08-15
**Read-only.** Žiadny mutate, žiadny rewrite GAQL vo workeroch.
**Login MCC:** `7024414113` (guard ako pri seede). Child účty: RK A `3726370609`, RK B `2272781649`.
**Agency / account (DB connect smoke):** Demo `b101361c-e250-4c43-b099-52c4febeb450` / `40a02a8e-7e31-439e-aecd-11aec040b2a2`
**Store:** in-memory (S0.4/S0.5 port). Persistencia do acquisition_campaigns sa v tomto behu nespúšťala.

## Verdikt

**PASS s nálezmi.** Očakávané kampane sú nažive, PAUSED, cross-tenant izolované. Produktový GoogleAdsClient.search() bije na zlé REST URL. Search-terms GAQL padá na dátovom filtri — report, nie prepis.

## Očakávané vs namerané

| Tenant | customer_id | Očakávaná kampaň | Namerané |
|---|---|---|---|
| RK A | 3726370609 | `24134657673` RKA-test-byty | nájdená, status PAUSED, 1 ad group, 1 RSA |
| RK B | 2272781649 | `24134894838` RKB-test-domy | nájdená, status PAUSED, 1 ad group, 4 keywords, 1 RSA |

RK A keyword sync vrátil 50 keyword riadkov, nie 4. Seed pri RK A bol SKIP (kampaň existovala skôr) — extra keywords sú z pôvodnej kampane, nie z dnešného RKB seedu.

### Cross-tenant

| Kontrola | Výsledok |
|---|---|
| RK A store obsahuje `24134894838` (RKB) | nie |
| RK B store obsahuje `24134657673` (RKA) | nie |
| izolácia | PASS |

### Workery (oficiálny REST customers/{id}/googleAds:search, v25)

| Worker | RK A | RK B |
|---|---|---|
| syncGoogleCampaigns | fetched 1 / upserted 1 | fetched 1 / upserted 1 |
| syncGoogleAdGroups | fetched 1 / upserted 1 | fetched 1 / upserted 1 |
| syncKeywords | fetched 50 / inserted 50 | fetched 4 / inserted 4 |
| syncMetrics | fetched 0 (PAUSED, LAST_7_DAYS prázdne) | fetched 0 |
| syncSearchTerms | 400 | 400 |
| extra RSA ad_group_ad (nie worker) | count 1 | count 1 |

## Nález 1 — GoogleAdsClient.search() URL (nie GAQL pole)

Native client volá POST /v25/customers/{id}:search → HTML 404 (Google error page). Seed aj tento beh používajú oficiálne POST /v25/customers/{id}/googleAds:search, ktoré žije.

Workery som neprepisoval. Adapter v one-off runneri len premapoval search() na správny REST path, aby S0.4/S0.5 mapovanie dostalo živé riadky. Produktový client ostáva 404, kým nepôjde samostatný PR na googleAds:search.

## Nález 2 — search-terms GAQL (pole/dátum, bez prepisu)

SEARCH_TERM_GAQL v apps/crm/src/lib/acquisition/sync/search-terms.ts selektuje segments.date bez date-range filtra.

Živá v25 odpoveď (RKB, bez secrets):

```text
HTTP 400 INVALID_ARGUMENT
queryError: EXPECTED_FILTERS_ON_DATE_RANGE
message: Expects filters on the following field to limit a finite date range: 'segments.date'
requestId: 3e3qPj_OZvynixF0xysh0g
```

Podľa zadania: report, nie prepis workera.

## Čo toto nie je

- Nie je to HTTP/cron sync v CRM. Žiadna produktová routa sa nespúšťala.
- Žiadny write do Google Ads.
- Žiadny PASS na L19 dashboard.