# PR #471 CI — Lint, test, build FAIL

**PR:** [#471](https://github.com/onlinovosk-bit/RealitkaAI/pull/471) `cursor/dodane-od-10-08-db1f`  
**Padnutý job:** [33009438387](https://github.com/onlinovosk-bit/RealitkaAI/actions/runs/33009438387/job/98311578254) (26. 8. 2026 20:20Z)  
**Zmena v tomto PR:** jeden súbor `docs/reports/2026-08-26-dodane-od-10-08.md` (docs-only).

## Zlyhanie (verbatim)

```
FAIL  tests/rls/valuation-tenants-rls.test.ts > valuation_tenants public RLS boundary > returns only enabled branding to anon and never exposes leads
AssertionError: expected null to match object { code: '42501', …(1) }
- Expected:
{
  "code": "42501",
  "message": "permission denied for table leads",
}
+ Received:
null
```

Anon `SELECT` na `public.leads` vrátil `error: null` a 0 riadkov (RLS filter), nie table-privilege `42501`.

## Koreň

Leftover `GRANT SELECT` pre `anon`/`PUBLIC` na `public.leads`. PostgREST pretiekol cez RLS a vrátil prázdny success namiesto permission denied. **Tento PR to nespôsobil** — rovnaký fail bol na `main` pri SHA `3357a528` (base v čase merge 26. 8.).

Oprava je už na `main`: `a4f58ff1` / [#489](https://github.com/onlinovosk-bit/RealitkaAI/pull/489) — `apps/crm/supabase/migrations/20260827214500_leads_revoke_anon_table_privileges.sql`.

## Zásah

Merge aktuálneho `origin/main` (`4d14b198`) do vetvy. Unique diff ostáva report pre makléra + tento záznam. Žiadna zmena RLS/testov v tomto PR.

## CI po merge (`a50705b3`)

`Lint, test, build` **PASS** (8m17s) — [34031309264](https://github.com/onlinovosk-bit/RealitkaAI/actions/runs/34031309264/job/101481208688).  
Vercel Preview Ready, Memory Engine PASS, Snyk PASS. Playwright smoke **SKIPPED** (docs-only, rovnaké ako pred fixom). PR je `MERGEABLE`.
